# SimpaiViewer 软件开发进度说明

> 编写日期：2026-08-14
> 用途：供其他部门 / 后续开发者接手继续开发。
> 语言要求：本项目的所有交付与沟通统一使用**简体中文**（用户明确要求，英文看不懂）。

---

## 一、项目概述

**SimpaiViewer** 是一个面向创作者的 Windows 图片管理 + AI 反推工具，基于 `DiffusionToolkit`（MIT 协议）fork 而来，技术栈为 **.NET 10 / WPF / C#**。

- 主仓库：`Simpai-Viewer`（GitHub: `lilesswoo-ai/Simpai-Viewer`）
- 本地代码根：`E:\workbuddy\2026-08-02-21-42-20\SimpaiViewer.NET\`
- 解决方案：`SimpaiViewer.sln`
- 主工程：`Diffusion.Toolkit\`（WPF 应用，输出 `SimpaiViewer.exe`）

**架构要点（重要）**：AI 能力不是直接塞进 C#，而是拆成一个**独立的 Python 服务——SimpaiAI Sidecar（FastAPI，端口 `:8765`）**作为唯一 AI 网关。C# 端只做：薄 HTTP 客户端 + UI + 本地 SQLite 资产库。这样把模型许可证风险、Python 依赖与 .NET 主程序**彻底隔离**。

```
┌─────────────────────┐      HTTP :8765       ┌──────────────────────────┐
│  SimpaiViewer (C#)  │  ───────────────────▶ │  SimpaiAI Sidecar (Py)  │
│  UI + SQLite + HTTP │ ◀─────────────────── │  FastAPI 反推/解构/调色  │
└─────────────────────┘      JSON 结果        └──────────────────────────┘
```

---

## 二、已完成工作（按版本）

### v2.0.1 — AI 反推 / 画面解构工作台（已发布）
- 本地优先的 AI 反推能力（选图 → 反推 → 显示 → 保存 单链打通）。
- **元数据优先（Metadata-first）**：自家生成图若已内置真实 Prompt，直接命中返回，0 成本、100% 准确。
- **9 维画面解构**：主体 / 环境 / 构图 / 光影 / 色彩 / 机位 / 镜头 / 材质 / 情绪 / 风格 / 姿态。
- **主色卡**：OpenCV + KMeans 提取主导色（不依赖视觉模型）。
- **创作型反推**：阶段 1 VLM 理解 → 阶段 2 LLM 合成 Reverse Prompt / Negative / 关键词。
- **本地资产沉淀**：结果写入 SQLite `ai_image_analysis` 表。
- 设置页新增「AI 反推」标签（启用开关、Sidecar 地址、超时、测试连接、默认 Provider/Skill）。
- 状态：已构建、已发布（`SimpaiViewer_v2.0.1_win_x64.zip`）。

### v2.0.2 — 内置一键启动 Sidecar + 离线容错（已发布）
1. **设置页「AI 反推」新增启动能力**
   - 「启动 Sidecar」「停止 Sidecar」按钮：直接拉起 / 关闭 `<程序>/sidecar/` 下的 Python 服务（`:8765`），无需手敲命令行。
   - 实时**状态显示**：运行中 / 未运行 / 进程在运行但无响应 / 失败原因。
   - 新增 `Sidecar 目录` 配置（默认 `sidecar`）。
2. **自动启动**：`AutoStartSidecar`（默认开），打开软件时若 Sidecar 未响应则后台自动拉起，不阻塞启动。
3. **Sidecar 随软件部署**：SimpaiAI 代码部署到 `sidecar/`，并内置 `.venv`（fastapi / uvicorn / opencv / numpy / jinja2 等），**Mock provider 可完全离线**跑通整条反推链路。
4. **离线容错修复**：`select_providers` 在缺少 `torch`/`transformers`（精简 venv）时自动跳过本地模型 provider、回退 `mock`，保证默认配置下反推/解构即返回 200。
5. **代码与发布状态**
   - 提交 `master`：已推送（最新提交 `4548e0e` 为 CHANGELOG 说明）。
   - 标签 `v2.0.2`：已推送 GitHub。
   - **I 盘部署**：`I:\AI\SimpaiViewer_v2.0.2\`（655 MB，含 venv，已排除 24 GB 模型权重——因 I 盘仅剩 51 GB）。
   - **GitHub Release**：已创建（`https://github.com/lilesswoo-ai/Simpai-Viewer/releases/tag/v2.0.2`），资产为 `SimpaiViewer_v2.0.2_win_x64.tar.gz`（241 MB，已排除 24 GB 权重，符合 GitHub 2 GB 单文件上限；Windows 可用 7-Zip / Win11 原生解压）。

---

## 三、进行中 / 待开发工作

### 3.1 图标与主题（v2.0.3 计划，尚未开工）【最高优先级】
**用户需求**：根据 `I:\AI\SimpaiViewer_v2.0.1\设计图\` 下的素材：
1. **修改软件图标（含 exe 图标）**——用设计稿里的图标替换现有图标。
2. **新增一套主题 UI**——只参考设计图的**配色、文字风格、左上角软件图标、名称字体风格**；**不要**完全按设计图重做整个界面结构。

**设计资产位置**（`I:\AI\SimpaiViewer_v2.0.1\设计图\`）：
- `simpai viewer UI设计稿.png` — 整体 UI 视觉（深色背景 + 紫/粉/青渐变强调色）。
- `图标设计稿.png` — 图标多尺寸/多底色变体规范。
- `软件图标.png` — **主图标**（圆角方 + 眼睛 Logo，建议作为 exe 图标源）。
- `软件图标 白底.png` — 去底 Logo（适合标题栏小图标）。

**关键接入点（已在代码中确认）**：
- 现有 exe 图标：`Diffusion.Toolkit\simpai-viewer.ico`（csproj 第 11 行 `<ApplicationIcon>simpai-viewer.ico</ApplicationIcon>`）。**替换为设计图标的多尺寸 .ico 即可**，无需改其它代码即可换 exe/任务栏图标。
- 标题栏 Logo + 名称：在 `MainWindow.xaml` 的 `<local:BorderlessWindow.TitleContent>` 区域（约 56–353 行），当前是 `<Label>SimpaiViewer</Label>`；按设计把图标 + "Simpai" 实色 + "Viewer" 渐变 塞进去。
- 主题体系：`Themes/Dark.xaml`、`Themes/Light.xaml`、`Themes/Common.xaml` 等，由 `Themes/ThemeManager.cs` 的 `ChangeTheme(themeName)` 切换（按文件名加载 `Themes/{themeName}.xaml`）。**新增主题 = 新建一个 `Themes/Simpai2.xaml`（或扩展 Dark.xaml）+ 在 ThemeManager 注册**。

**环境约束（接手前必读）**：
- 当前自动化环境**无法读取图片内容**（Read 工具对图片返回"不支持"）。因此配色不能靠肉眼，必须**用 Python 从 `simpai viewer UI设计稿.png` 程序化提取真实色值**（PIL/numpy 量化取主色 + 高饱和强调色），再写进 XAML 画刷。下面给了一条可直接用的提取命令思路：
  ```python
  # 用 sidecar 的 .venv（已含 pillow/numpy）跑
  from PIL import Image
  import numpy as np
  img = np.asarray(Image.open(r"I:\AI\SimpaiViewer_v2.0.1\设计图\simpai viewer UI设计稿.png").convert("RGB"))
  img = img.reshape(-1,3)
  # 1) 取最暗且最高频的若干色 → 背景/面板
  # 2) 取高饱和度的若干色 → 渐变强调色（紫/粉/青）
  ```
- 图标转 .ico：用 PIL 把 `软件图标.png` 重采样到 16/24/32/48/64/128/256 并 `save(..., format="ICO")`，产出多尺寸 .ico。

### 3.2 待收尾 / 已知事项
- **SimpaiAI 源仓库不在本机**：`dist/SimpaiViewer_v2.0.2/sidecar/` 是**部署副本**，`app/providers.py` 里的离线回退修复只在此副本中。下次用 robocopy 重建 `dist` 前，必须先把该修复**并入 SimpaiAI 源仓库**，否则会在重建时被覆盖。
- **24 GB 本地模型权重未进 GitHub 发布包**（超限）。本地 VLM 模式启用步骤：`sidecar/.venv` 内 `pip install torch transformers` + 按 `models/manifest.json` 下载权重（JoyCaption / Qwen2.5-VL）。I 盘当前为"无权重"部署，Mock 默认即用。
- **I 盘空间紧张（仅 51 GB 可用）**：完整 25 GB（含权重）副本暂未放入 I 盘；如需本地 VLM，先在 I 盘腾出 ≥25 GB 再 robocopy 全量。
- **`.zip` 发布包**：因会话中断多次丢失后台构建，最终以 `.tar.gz` 发布（功能等价）。如坚持要 `.zip`，用 PowerShell `Compress-Archive -Exclude *.safetensors` 重打即可。

---

## 四、关键文件索引（接手速查）

| 类别 | 文件 | 说明 |
|------|------|------|
| 启动器 | `Diffusion.Toolkit/AI/SidecarLauncher.cs` | 单例管理 Sidecar 进程：解析 `sidecar/` 目录、找 `.venv/python.exe`、启动/停止、健康探测 |
| 设置 | `Diffusion.Toolkit/AI/AiSettings.cs` | `SidecarPath`、`AutoStartSidecar` 等配置项 |
| 设置页 UI | `Diffusion.Toolkit/Pages/Settings.xaml` + `.xaml.cs` | 「启动/停止 Sidecar」按钮、状态文本、Sidecar 目录输入框（"AI 反推"标签） |
| 自动启动 | `Diffusion.Toolkit/MainWindow.xaml.cs` | `TryAutoStartSidecar()`，在 `OnLoaded` 调用 |
| 主题管理 | `Diffusion.Toolkit/Themes/ThemeManager.cs` | `ChangeTheme(name)` 按文件名加载 `Themes/{name}.xaml` |
| 主题资源 | `Diffusion.Toolkit/Themes/*.xaml` | Dark / Light / Common / Window / Menu / Scrollbars / ToolTips / SWStyles |
| 应用入口 | `Diffusion.Toolkit/App.xaml` | 合并 Dark+Common+SWStyles+Window 字典 |
| 图标 | `Diffusion.Toolkit/simpai-viewer.ico` | 当前 exe 图标（**待替换为设计图标**） |
| 标题栏 | `Diffusion.Toolkit/MainWindow.xaml` | `BorderlessWindow.TitleContent`（约 56–353 行）放 Logo + 名称 |
| 资源目录 | `Diffusion.Toolkit/Icons/`、`Images/` | 现有界面图标/图片资源 |
| Sidecar 主程序 | `dist/.../sidecar/app/main.py` | `/health`、`/v1/reverse-prompt`、`/v1/deconstruct`、`/v1/providers`、`/v1/skills` |
| 路由/回退 | `dist/.../sidecar/app/providers.py` | `select_providers`（离线自动回退 Mock）、`get_provider` |
| 配置 | `dist/.../sidecar/app/config.py` | `_default_providers()`：local-joycaption / local-qwen / mock |
| 流水线 | `dist/.../sidecar/app/pipeline.py` | 元数据优先 → caption → 9 维解构 → 调色 → 阶段2 合成 |
| 模型清单 | `dist/.../sidecar/models/manifest.json` | 本地模型声明（JoyCaption / Qwen2.5-VL） |

---

## 五、构建与部署流程

**构建（C# 单文件自包含）**
```bash
cd SimpaiViewer.NET
dotnet build SimpaiViewer.sln -c Release -p:DisableBuildServers=true   # 0 错误即合格
dotnet publish Diffusion.Toolkit/Diffusion.Toolkit.csproj -c Release \
  --self-contained true -p:PublishSingleFile=true \
  -o dist/SimpaiViewer_vX.Y.Z
```
> 注意：构建前建议 `taskkill /F /IM dotnet.exe` 并清 `obj/bin`，可规避 lock 冲突。

**部署 Sidecar（robocopy 进 dist）**
```bash
robocopy <SimpaiAI源> dist/SimpaiViewer_vX.Y.Z/sidecar /E /XD venv __pycache__ .git
# 然后在 sidecar/ 内建 .venv 并装最小依赖（见 requirements.txt 的精简子集）
```

**Sidecar 运行（或在软件内点「启动 Sidecar」）**
```bash
cd dist/SimpaiViewer_vX.Y.Z/sidecar
.venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8765
```

**发布**
- GitHub Release 资产需 **< 2 GB**：用 `tar --exclude='*/sidecar/models' -czf ...` 或 PowerShell `Compress-Archive -Exclude *.safetensors` 排除 24 GB 权重。
- 标签与 `master` 推送后，`gh release create vX.Y.Z --latest --notes-file notes.md <资产>`。

---

## 六、已知问题与风险

1. **Sidecar 离线回退修复未并入 SimpaiAI 源仓库**（见 3.2）——重建 dist 会丢。
2. **GitHub 大文件**：24 GB 权重无法进 Release，需文档说明本地 VLM 启用方式。
3. **环境读图限制**：自动化侧无法用视觉看设计图，配色须程序化提取。
4. **进程/端口管理**：Sidecar 用 `:8765`，调试时若上次进程没退干净会 `winerror 10048`；用 `netstat -ano | grep :8765` + `taskkill` 清理（PowerShell 用 `Stop-Process -Id`）。
5. **I 盘空间**：仅 51 GB，放不下含权重的完整副本。

---

## 七、给其他部门的接手建议

1. **下一步最该做**：v2.0.3 的**图标替换 + 新主题**（资产齐全、接入点已定位，见 3.1）。先程序化提色值，再写 `Themes/Simpai2.xaml` 并替换 `simpai-viewer.ico`。
2. **不要动整体界面结构**：用户明确只要换肤 + 换图标，沿用现有 `BorderlessWindow` + 现有控件样式。
3. **改动 Sidecar Python 后**：记得同步回 SimpaiAI 源仓库，不要只改 `dist` 副本。
4. **所有反馈用中文**。
5. **版本号**：`Diffusion.Toolkit/version.txt` + `.csproj` 的 `AssemblyVersion/FileVersion/AssemblyInformationalVersion` 三处同步改；改完在 `CHANGELOG.md` 加一节。

---

## 八、一句话总结

v2.0.1 打通 AI 反推工作台、v2.0.2 内置一键启动 Sidecar 并实现离线容错（已发布），**下一步是 v2.0.3 的图标替换 + 新主题**（资产在 `I:\AI\SimpaiViewer_v2.0.1\设计图\`，接入口与设计色值提取方法见本文第三节）。
